{
  "name": "fab",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "NODE_ENV=test mocha tests --reporter mocha-better-spec-reporter"
  },
  "dependencies": {
    "express": "^4.12.4",
    "socket.io": "^1.3.5"
  }
}
